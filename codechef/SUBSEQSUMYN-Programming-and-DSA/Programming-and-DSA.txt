bool existsSubsequence(vector<int>& nums,int n, int k) {    // write your code here    //BASE case,, invariant    if(k==0){        return true;    }    if(k<0||n==0){        return false;    }            //RECURSIVE        //assume arr[n] is part of k sum         bool pick = existsSubsequence(nums,n-1,k-nums[n-1]);        //else        bool notpick = existsSubsequence(nums,n-1,k);        return pick||notpick;                    }    