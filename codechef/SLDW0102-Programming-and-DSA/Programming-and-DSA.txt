#include <bits/stdc++.h>using namespace std;int main() {    // your code goes here        int K ,N;                std::cin>>N>>K;        std::vector<int> arr(N);        int L=0;        int R=K-1;        int sum =0;        for(int i=0;i<N;i++){            std::cin>>arr[i];            if(i<K){                sum+=arr[i];            }        }        int Max = sum;        while(R<N-1){            R++;                         sum +=arr[R];              sum -=arr[L];                        L++;            if(sum>Max){                Max = sum;            }        }        std::cout<<Max;        return 0;}