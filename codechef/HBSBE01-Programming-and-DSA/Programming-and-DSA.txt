vector<vector<int>> memo;int dp(int i,int j,vector<long long>& childrenGreedFactors,vector<long long>& cookieSizes){    if(i<0||j<0){        return 0;    }    if(memo[i][j]!=-1){        return memo[i][j];    }    int ans = dp(i-1,j,childrenGreedFactors,cookieSizes);    ans = max(ans,dp(i,j-1,childrenGreedFactors,cookieSizes));    if(childrenGreedFactors[i]<=cookieSizes[j]){         ans = max(ans,1+dp(i-1,j-1,childrenGreedFactors,cookieSizes));    }    return memo[i][j] =ans;}int findMaxContentChildren(vector<long long>& childrenGreedFactors,                           vector<long long>& cookieSizes) {    sort(childrenGreedFactors.begin(),childrenGreedFactors.end());     sort(cookieSizes.begin(),cookieSizes.end());    if(childrenGreedFactors.size()==0||cookieSizes.size()==0){        return 0;    }    memo.assign(childrenGreedFactors.size(),vector<int>(cookieSizes.size(),-1));    return dp(childrenGreedFactors.size()-1,cookieSizes.size()-1,childrenGreedFactors,cookieSizes);}