#include <bits/stdc++.h>using namespace std;int main() {    // your code goes here    int T;    std::cin>>T;    while(T--){        int N,K;        std::cin>>N>>K;        int L=0;        int R=K-1;        vector<int> arr(N);        int sum =0;        for(int i=0;i<N;i++){            std::cin>>arr[i];            if(i<K){                sum += arr[i];            }        }        int max=sum;        while(R+2<=N){            R++;            sum += arr[R];            sum -= arr[L];                        L++;            if(max<sum){                max = sum;            }        }               std::cout<<max<<endl;            }    }